# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: FlutterAudioDiscoveryBindings
description: |
  Bindings for `src/flutter_audio_discovery.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/flutter_audio_discovery_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_audio_discovery.h'
  include-directives:
    - 'src/flutter_audio_discovery.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
